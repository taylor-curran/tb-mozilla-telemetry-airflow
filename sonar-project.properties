# SonarCloud project configuration
sonar.organization=taylor-curran
sonar.projectKey=taylor-curran_tb-mozilla-telemetry-airflow

# Exclude non-testable infrastructure and DAG files from coverage analysis
sonar.coverage.exclusions=dags/**,jobs/**,plugins/**,tests/**,Dockerfile,docker-compose.yml
